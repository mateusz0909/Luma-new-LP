## 2026-09-01T18:11:22Z
<USER_REQUEST>
You are the Technical HTML & SSG Explorer.
Your working directory is `c:\Users\m.byrtus\Documents\Projects\zzz_other\Luma-new-LP\.agents\survey_codebase_1`.
You MUST read `c:\Users\m.byrtus\Documents\Projects\zzz_other\Luma-new-LP\.agents\ORIGINAL_REQUEST.md` first.

Tasks:
1. Explore the entire codebase architecture: `package.json`, dependencies, build scripts, `index.html`, Vite/React config, prerender/SSG scripts, server or build pipelines, image assets.
2. Audit current technical SEO setup:
   - `<title>` and `<meta name="description">`
   - Canonical URLs, robots meta tags, viewport, charset, lang
   - Open Graph (`og:title`, `og:description`, `og:image`, `og:url`, `og:type`, `og:site_name`, `og:locale`) and Twitter Card tags
   - Schema.org structured data JSON-LD (SoftwareApplication, Organization, WebSite, FAQPage, etc.)
   - Image `alt` attributes, SVG accessibility (`aria-hidden`, titles), web vitals/performance tags
   - Prerender / SSG setup: how `npm run build` works, how static HTML is generated, whether `dist/index.html` (and any subpages) contains full prerendered HTML content or only empty root container.
3. Identify all exact files to modify and technical requirements to satisfy R2 and R3.
4. Update your `progress.md` frequently.
5. Write your comprehensive survey report to `c:\Users\m.byrtus\Documents\Projects\zzz_other\Luma-new-LP\.agents\survey_codebase_1\handoff.md`.
6. Use `send_message` to notify the orchestrator when finished.
</USER_REQUEST>
